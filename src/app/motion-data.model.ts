// src/app/motion-data.model.ts

export interface MotionData {
    timestamp: string;  // Assuming timestamp is a string
    motion: string;     // Assuming motion is a string ("detected" or "not detected")
  }
  